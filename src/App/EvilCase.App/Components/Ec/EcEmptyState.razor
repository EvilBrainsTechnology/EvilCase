@* The call to action SDD-020 allows arrives with the first screen that offers one. *@
<div class="ec-empty">
    <EcIcon Paths="@this.Icon" Size="EcIconSize.Large" />
    <p class="ec-empty-text">@this.Text</p>
</div>

@code {
    [Parameter, EditorRequired]
    public required string Icon { get; set; }

    [Parameter, EditorRequired]
    public required string Text { get; set; }
}
