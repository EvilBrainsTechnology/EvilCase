# Local Claude Code state
.claude/worktrees/
.claude/settings.local.json

# Local secrets. Ignored from the repository root because the file is searched for upwards from the
# host output directory, so a .env anywhere above it is picked up and must never be committed.
.env*
!.env.example
