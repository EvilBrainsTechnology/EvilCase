@using EvilBrains.EvilCase.Api.Contract.Cases

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Podřízené spisy</h3>
        <div class="card-actions">
            <a href="@($"/cases/new?parent={ParentCaseId}")" class="btn btn-primary">Nový podřízený spis</a>
        </div>
    </div>

    @if (Items.Count == 0)
    {
        <div class="empty">
            <p class="empty-title">Žádné podřízené spisy</p>
        </div>
    }
    else
    {
        @* Table on desktop, cards on mobile: two renderings switched by CSS only. *@
        <div class="d-none d-lg-block">
            <table class="table card-table table-vcenter">
                <thead>
                    <tr>
                        <th>Spisová značka</th>
                        <th>Název</th>
                        <th>Stav</th>
                        <th>Datum</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Items)
                    {
                        <tr>
                            <td class="text-nowrap">@item.CaseNumber</td>
                            <td><a href="@($"/cases/{item.Id}")">@item.Title</a></td>
                            <td class="text-nowrap">
                                <Status BackgroundColor="CaseStatusDisplay.Color(item.Status)" Lite="true">@CaseStatusDisplay.Text(item.Status)</Status>
                            </td>
                            <td class="text-nowrap">@DateDisplay.Text(item.Date)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-body d-lg-none">
            @foreach (var item in Items)
            {
                <CaseListCard Case="item" />
            }
        </div>
    }
</div>

@code {
    [Parameter, EditorRequired]
    public required IReadOnlyList<CaseListItem> Items { get; set; }

    [Parameter, EditorRequired]
    public required Guid ParentCaseId { get; set; }
}
