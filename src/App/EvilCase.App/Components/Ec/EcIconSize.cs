namespace EvilBrains.EvilCase.App.Components.Ec;

public enum EcIconSize
{
    /// <summary>
    /// 14 px, inside a dense row.
    /// </summary>
    Small = 0,

    /// <summary>
    /// 16 px, inside a button.
    /// </summary>
    Medium = 1,

    /// <summary>
    /// 18 px, inside the menu.
    /// </summary>
    Large = 2,
}
